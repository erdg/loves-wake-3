import React from 'react';
import Typography from '@material-ui/core/Typography';

const Search = (props) => {
   return (
      <div>
         <Typography
            variant="display1"
         > Search Memorials (component)
         </Typography>
      </div>
   )
}

export default Search;
