import { createMuiTheme } from '@material-ui/core/styles';

const theme = createMuiTheme({
   palette: {
      primary: {
         light: '#E0E4E5',
         main: '#632B6C',
         dark: '#280F36'
      },
      secondary: {
         light: '#FFD9C4',
         main: '#FFC1A0',
         dark: '#FE9C7F'
      },
   }
})

export default theme;
